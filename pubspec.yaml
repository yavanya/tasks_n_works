name: tasks_n_works
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  stacked:
  stacked_services:

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart:
  build_runner:
  stacked_generator:

flutter:

  uses-material-design: true
